# MQTT (docker network defaults)
MQTT_HOST=mosquitto
MQTT_PORT=1883
MQTT_USER=
MQTT_PASS=

# For local runs, uncomment the lines below:
# MQTT_HOST=127.0.0.1

# Align with policy_brain + act/cmd command bus
TOPIC_FRAME=self_gaming/vision/frame
TOPIC_INTENT=policy_brain/cmd
# If act_agent is running, it expects an "action" field in the payload.
TOPIC_ACTION=act/cmd
TOPIC_TELEM=self_gaming/telemetry/nitrogen

# NitroGen inference server (ZeroMQ)
NITROGEN_HOST=nitrogen
NITROGEN_PORT=5555
# fp16 saves memory on Jetson; use fp32 if you hit accuracy/compat issues.
NITROGEN_DTYPE=fp16
# For local runs, uncomment the line below:
# NITROGEN_HOST=127.0.0.1
NITROGEN_TIMEOUT_S=5.0

# Rate limit outgoing actions (Hz). Raise to 15 only if safe.
ACTION_RATE_HZ=5
